%! Program to draw the Post"script" icon.
%
% The workbench screen has only two bitplanes, so the third plane (yellow)
% is ignored.  The RGB values are inverted, and the planes are in reverse
% order.
%
/BLUE   {1 1 0 setrgbcolor} def
/WHITE  {0 1 0 setrgbcolor} def
/BLACK  {1 0 0 setrgbcolor} def
/ORANGE {0 0 0 setrgbcolor} def
%
1.5 .8 scale
WHITE
clippath fill
BLACK
/Times-Roman findfont 24 scalefont setfont
20 40 moveto (Post) show
ORANGE
/Alison findfont 24 scalefont setfont
20 16 moveto (\252) show -5 0 rmoveto (script) show 6 0 rmoveto (\272) show
BLUE
16 8 moveto 74 0 rlineto 0 53 rlineto -74 0 rlineto closepath
2 setlinewidth
stroke
showpage

